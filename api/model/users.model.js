export const User = class User {
  constructor(userId, username, email, password, name, coverPi, profilePi, city, website) {
    this.userId = userId;
    this.userId = username;
    this.email = email;
    this.password = password;
    this.name = name;
    this.coverPi = coverPi;
    this.profilePi = profilePi;
    this.city = city;
    this.website = website;
  }
};